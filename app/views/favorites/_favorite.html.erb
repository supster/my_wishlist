<%= form_for(current_user.favorites.build(vendor_id: vendor_obj.id), remote: true) do |f| %>
	<%= hidden_field_tag :vendor_id, vendor_obj.id %>
	<%= f.submit "Add to My Favorite", class: "btn btn-small btn btn-info" %>
<% end %>
