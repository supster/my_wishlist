<% if signed_in? %>
	<div id="fav_form">
		<% if current_user.favorite?(@vendor.id)  %>
			<%= render "favorites/favorite"%>
		<%else%>
			<%= render "favorites/unfavorite"%>
		<% end %>
	</div>
<% end %>
