<%= form_for(current_user.favorites.find_by_vendor_id(@vendor), html: { method: :delete }, remote: true) do |f| %>
	<%= hidden_field_tag :vendor_id, @vendor.id %>
	<%= f.submit "Remove from My Favorite", class: "btn btn-small" %>
<% end %>			
