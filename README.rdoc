- Uses partials - render “”
- Includes google maps API (either map or geocoder) 
- includes an Ajax request
- Is able to create, edit and remove records
- Uses a “has many” association
- Uses a “has and belongs” to many association


by Supatra Indharameesup
