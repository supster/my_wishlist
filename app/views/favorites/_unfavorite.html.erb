<%= form_for(current_user.favorites.build(vendor_id: @vendor.id), remote: true) do |f| %>
	<%= hidden_field_tag :vendor_id, @vendor.id %>
	<%= f.submit "Add to My Favorite", class: "btn btn-small btn btn-info" %>
<% end %>
